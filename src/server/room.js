/**
 * Created by jacques on 10-11-16.
 */
// actually , require("../game.js");
var game;

function createRoom(name,creatorId,callback) {
    callback(null,room);
}

module.exports = {
    createRoom : createRoom
};